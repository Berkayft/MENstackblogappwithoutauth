<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1><%= user.username %></h1>
    <div class="blogs">
        <% blogs.forEach(element => { %>
            <h1><%= element.title %></h1>
            <a href="/blog/list/<%= element._id %>">... see more</a>
        <% }) %>
    </div>
</body>
</html>